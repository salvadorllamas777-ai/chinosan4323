PORT=3000
# Use a real MongoDB URI in production; leave blank to use in-memory in development/test
MONGO_URI=
JWT_SECRET=replace_this_with_a_strong_secret
APP_URL=http://localhost:3000
# Email settings for production SMTP
EMAIL_HOST=
EMAIL_PORT=587
EMAIL_SECURE=false
EMAIL_USER=
EMAIL_PASS=
EMAIL_FROM=
NODE_ENV=development

